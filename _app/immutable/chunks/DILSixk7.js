import"./DsnmJJEf.js";import"./69_IOA4Y.js";import{p as I,c as d,r as u,s as i,E as _,g,a as q,f as S,Z as y}from"./B-xle7iY.js";import{f as l,a as h}from"./DR_GZfaw.js";import{s as w}from"./DukKBkwU.js";import{e as F,i as A}from"./m2c-ilOh.js";import{s as G}from"./CdQoudc1.js";import{I as b}from"./aLgAakBz.js";var H=l('<div class="svelte-15kned"><div class="number svelte-15kned"> </div> <div class="text svelte-15kned"> </div></div>'),P=l('<div class="instructions svelte-15kned"></div>');function v(c,r){I(r,!0);function p(t){return t.trim().split(/\r?\n/).map(e=>{const[o,a]=e.split(" ").map(s=>s.trim());return{color:o,number:a}})}function m(t){return t.number=="..."?"...":`Instruction ${String.fromCharCode(64+parseInt(t.number))}`}var n=P();F(n,21,()=>p(r.instructionsText),A,(t,e)=>{var o=H();let a;var s=d(o),k=d(s,!0);u(s);var f=i(s,2),T=d(f,!0);u(f),u(o),_(x=>{a=G(o,"",a,{"background-color":g(e).color}),w(k,g(e).number),w(T,x)},[()=>m(g(e))]),h(t,o)}),u(n),h(c,n),q()}var W=l('<p>In this blogpost, some simplifications have been made to keep things simple (for examples, <a href="https://en.wikipedia.org/wiki/Multi-core_processor" rel="nofollow" target="_blank">multi-core processors</a> and <a href="https://en.wikipedia.org/wiki/Hyper-threading" rel="nofollow" target="_blank">hyper-threading</a> do make it possible for computers to do multiple things at the same time, contrarily to what this blogpost will claim), but the explained difference between synchronous programming and asynchronous programming is still accurate.</p>'),O=l("<p>Asynchronous programs run faster when there is concurrent work that needs to be done. If you have no concurrent work that needs to be done, then synchronous programs run faster, since they don't contain the overhead of a task list. In practice, it is often only small scripts that don't contain any concurrent work, so most programs can benefit from asynchronous programming.</p> <p>Also note that asynchronous programs often are a little bit harder to implement. No pain, no gain!</p>",1),$=l(`<p>Many programming languages these days offer two different ways of doing nothing for <code>x</code> milliseconds:</p> <table border="1" style="margin: 1em auto;"><thead><tr><th>Language</th><th>Synchronous</th><th>Asynchronous</th></tr></thead><tbody><tr><td>C#</td><td><a href="https://learn.microsoft.com/en-us/dotnet/api/system.threading.thread.sleep?view=net-8.0" rel="nofollow" target="_blank">Thread.Sleep()</a></td><td><a href="https://learn.microsoft.com/en-us/dotnet/api/system.threading.tasks.task.delay?view=net-8.0" rel="nofollow" target="_blank">Task.Delay()</a></td></tr><tr><td>Kotlin</td><td><a href="https://docs.oracle.com/en/java/javase/22/docs/api/java.base/java/lang/Thread.html#sleep(long)" rel="nofollow" target="_blank">Thread.sleep()</a></td><td><a href="https://kotlinlang.org/api/kotlinx.coroutines/kotlinx-coroutines-core/kotlinx.coroutines/delay.html" rel="nofollow" target="_blank">delay()</a></td></tr><tr><td>Python</td><td><a href="https://docs.python.org/3/library/time.html#time.sleep" rel="nofollow" target="_blank">time.sleep</a></td><td><a href="https://docs.python.org/3/library/asyncio-task.html#asyncio.sleep" rel="nofollow" target="_blank">asyncio.sleep()</a></td></tr><tr><td>Etc.</td><td>...</td><td>...</td></tr></tbody></table> <p>As has been hinted, you must know the difference between synchronous programming and asynchronous programming to understand when to use which of the two versions. This blogpost aims to explain why it's important to use the right one, and what happens if you use the wrong one.</p> <!> <h2>Doing things "concurrently" on a computer</h2> <p>A computer can be seen as a very simple machine that can only execute one instruction at a time. This essentially means that only one program at a time can run on a computer, but the clever usage of <a href="https://en.wikipedia.org/wiki/Time-sharing" rel="nofollow" target="_blank">time-sharing</a> makes it possible to fool us humans into thinking that multiple programs are running at the same time.</p> <p>For example, if you want to run three programs at "the same time" on a computer, the computer will:</p> <ol><li>First run the first program for a few milliseconds.</li> <li>Then run the second program for a few milliseconds.</li> <li>Then run the third program for a few milliseconds.</li> <li>Then run the first program for a few milliseconds again.</li> <li>Then run the second program for a few milliseconds again.</li> <li>Etc.</li></ol> <p>The computer swaps between running the programs so fast, so we humans don't notice that it is happening!</p> <p>Below is a visualization of this, where the red, lime and yellow programs run "at the same time", and you can see the order the computer executes the instructions in the programs (first two instructions in red are executed, then 3 instructions in lime, then 2 instructions in yellow, etc.).</p> <!> <p>One big downside with this approach is that it takes quite a long time for the computer to swap between programs running. When the computer swaps program, it is actually the <a href="https://en.wikipedia.org/wiki/Operating_system" rel="nofollow" target="_blank">Operating System</a> (OS) program that has ordered the computer to execute some instructions to do the swapping, so a more accurate visualization of what actually has happened is something like this (where the aqua program is the OS program running that instructs the computer to do the swapping):</p> <!> <p>The computer still runs the programs so fast that we humans won't notice that it actually only runs one program at a time, but as you understand, the more often the computer is swapping program, the slower our programs will run, because much computation is "wasted" on doing the swapping, and not on running the programs.</p> <h2>Concurrency in a synchronous program</h2> <p>A program itself often needs to do multiple things concurrently. For example, <a href="https://giphy.com/explore/animated" rel="nofollow" target="_blank">a GIF image can be animated</a>, so a GIF image can consist of multiple images, and when it's displayed on the screen by a program, the program will:</p> <ol><li>First display the first image in the GIF on the screen.</li> <li>Then wait a few milliseconds.</li> <li>Then display the second image in the GIF on the screen.</li> <li>Then wait a few milliseconds.</li> <li>Then display the third image in the GIF on the screen.</li> <li>Then wait a few milliseconds.</li> <li>Etc.</li></ol> <p>The waiting here can be done using a synchronous <code>sleep()</code> call. That basically tells the OS "<em>I don't need to do anything for a few milliseconds</em>", and the OS can instruct the computer to swap to another program. Then, after a few milliseconds, the program will run again, and can instruct the computer to display the next image in the GIF on the screen, and so on.</p> <p>BUT, this basically means the program won't be able to do anything else but waiting and changing the image shown on the screen, because the program is stuck in this loop! So if the program would, for example, want to display multiple GIF images on the screen, only one of them would be updated! (unless one writes special code to update all the GIF images at the same time, but let's imagine that's not the case)</p> <p>So, what programs can do to avoid this problem is to start this "concurrent" work that needs to be done in new <a href="https://en.wikipedia.org/wiki/Thread_(computing)" rel="nofollow" target="_blank">threads</a>. For simplicity, a thread can simply be seen as a separate program. So, if our red program would want to display 10 GIF images, it would instruct the computer to start 10 programs, each responsible for animating respective GIF image. I won't even try to visualize that in a figure, because the figure would be very big xD</p> <p>So, even though we only have 3 "real" programs running, the computer will need to swap between 13 programs running! And with the swapping overhead from the OS program, this all becomes very, very slow, especially when other programs also wants to do some concurrent work too, so they will probably have a bunch of threads running as well. Does there exist a better way we can do this?</p> <h2>Concurrency in an asynchronous programming</h2> <p>So, creating many threads to wait for something turned out to be bad. What can we do instead? Well, this is where asynchronous programming comes into the picture.</p> <p>Just as the OS has a list of programs that want to run, each asynchronous program has its own list of tasks that should run when an <em>event</em> has happened. An event can for example be:</p> <ul><li>When a specific point in time is happening</li> <li>When the computer has an Internet connection (after having lost it)</li> <li>When some new content has been written to a file</li> <li>When the computer receives an HTTP request</li> <li>When the computer receives back an HTTP response to an HTTP request it has sent</li> <li>Etc.</li></ul> <p>So, a program doesn't need to create new threads to do the waiting, instead the program has a list of tasks that needs to be done, and the program carries out one of the tasks at a time (when the corresponding event has happened, or as soon as possible thereafter).</p> <p>This way, the computer is not overwhelmed with swapping program to run. Instead, each asynchronous program simply changes which task to run. And to change which task to run is much faster than changing which program to run, so asynchronous programs tends to run faster than synchronous programs, and this is the benefit with asynchronous programs!</p> <!> <h2>What happens if I use asynchronous programming in my synchronous program?</h2> <p>You can't really do this. To use asynchronous programming, your program must have a list of tasks it should carry out, and it will have that if you are able to use asynchronous programming in it, which makes it an asynchronous program.</p> <p>If you try to use an asynchronous function/method in a synchronous program/a synchronous part of your program, you will often get an error from your compiler/runtime engine to let you know that you can't use the function/method.</p> <h2>What happens if I use synchronous programming in my asynchronous program?</h2> <p>Ah, this is where it gets interesting. Nothing stops you from using synchronous programming in an asynchronous program. But it is very bad to do so. Let's take a look at an example to understand why.</p> <p>Imagine your asynchronous program has the following three tasks it should complete:</p> <ol><li>Draw the next image in a GIF on the screen.</li> <li>Handle a received HTTP response (from a previously sent HTTP request).</li> <li>Handle a mouse button click.</li></ol> <p>Your program starts by carrying out Task 1. In it, you draw the next image in the GIF on the screen, and then you need to add this very same task to the list again 1 second later (by default, tasks are deleted from the list when they have been carried out). Here you need to wait for 1 second, and you should use the asynchronous version of wait (often called <code>delay(1000ms)</code>). Then your program can continue with carrying out Task 2, and then Task 3, immediately after you have called <code>delay(1000ms)</code>.</p> <p>But, if you instead use the synchronous version of wait (often called <code>sleep(1000ms)</code>), then you tell the OS that your program does not need to run at all for 1 second, so even though Task 2 and Task 3 can run immediately after you have completed Task 1, they will not, because nothing in your program will be executed at all for 1 second. So, if you use synchronous programming in an asynchronous program, you prevent the program from being able to immediately run the other tasks it contains, so your program will overall run slower!</p> <h2>I don't use <code>sleep()</code>, do I need to care?</h2> <p><code>sleep()</code> is just the example I chose to go with. As I briefly mentioned, the benefit with asynchronous programming over synchronous programming (faster code execution) applies to all kind of waiting, and is not limited to <code>sleep()</code>. For example, all of the following operations includes waiting of some kind:</p> <ul><li>Write something to a file (waiting for the hard drive to report back a successful write)</li> <li>Read something from a file (waiting for the hard drive to report back the read content)</li> <li>Send an SQL query to a DB (waiting for the query to be sent to the DB over the Internet, then for the DB to execute the query, and then for the result to be sent back to the computer over the Internet)</li> <li>Send an HTTP request to a server (waiting for the HTTP request to be sent to the server over the Internet, then for the server to execute the request, and then for the HTTP response to be sent back to the computer over the Internet)</li> <li>Etc.</li></ul> <p>In all of these cases, you can benefit from using the asynchronous functions/methods instead of the synchronous ones.</p>`,1);function N(c){var r=$(),p=i(S(r),6);b(p,{title:"Simplifications have been made...",children:(e,o)=>{var a=W();h(e,a)},$$slots:{default:!0}});var m=i(p,14);v(m,{instructionsText:`
	red 1
	red 2
	lime 1
	lime 2
	lime 3
	yellow 1
	yellow 2
	red 3
	red 4
	lime 4
	yellow 3
	yellow 4
	red 5
	silver ...
`});var n=i(m,4);v(n,{instructionsText:`
	red 1
	red 2
	aqua 1
	aqua 2
	lime 1
	lime 2
	lime 3
	aqua 3
	aqua 4
	yellow 1
	yellow 2
	aqua 5
	aqua 6
	red 3
	red 4
	aqua 7
	aqua 8
	lime 4
	aqua 9
	aqua 10
	yellow 3
	yellow 4
	aqua 11
	aqua 12
	red 5
	silver ...
`});var t=i(n,30);b(t,{title:"Asynchronous programs aren't always faster...",children:(e,o)=>{var a=O();y(2),h(e,a)},$$slots:{default:!0}}),y(26),h(c,r)}export{N as default};
